apiVersion: v1
kind: Secret
metadata:
  name: openai-secret
  labels:
    app: sre-ai-copilot
type: Opaque
data:
  # Values must be base64 encoded manually or via Helm
  OPENAI_API_KEY: "{{ .Values.openaiApiKey }}"
