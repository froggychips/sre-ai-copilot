fastapi
uvicorn[standard]
anthropic
pydantic-settings
structlog
sqlalchemy
alembic
celery[redis]
# Kubernetes-клиент инцидент-копилота (app/diagnostics, app/remediation).
kubernetes
# python-jose was replaced by PyJWT in commit 680b3d0 (CVE-2024-33663 / CVE-2024-33664).
# Прямая зависимость (app/auth.py: import jwt) — держим явно, чтобы lock-реген её не дропал.
pyjwt
# Прямая зависимость (app/api/discord_interactions.py: Ed25519-проверка подписи
# Discord-вебхуков) — держим явно, иначе pip-compile/dependabot-реген дропает её
# из requirements.txt (она лишь транзитив google-auth) и CI/чистый venv падает
# ModuleNotFoundError. Прецедент: PR #161/#158 2026-06-23.
cryptography
opentelemetry-api
opentelemetry-sdk
opentelemetry-instrumentation-fastapi
opentelemetry-exporter-otlp
prometheus-client
psycopg2-binary
tenacity
# Прямые зависимости, раньше жившие только транзитивно (реген лока или
# обновление родителя молча их дропнет — тот же класс граблей, что pyjwt/
# cryptography выше):
#   httpx    — ~11 модулей (Seq/VM/AM/Discord-клиенты и др.)
#   pyyaml   — app/services/ownership_suggester.py (import yaml)
#   requests — scripts/squad_dashboard.py (гоняется из образа CronJob-ом
#              squad-dashboard; psycopg2 оттуда же покрыт psycopg2-binary)
httpx
pyyaml
requests
