id: 011_gate_preprod_defaults_to_block
title: preprod не перечислен в политике — значит блок по умолчанию
source: synthetic
llm: false
incident: {incident_id: golden-011, severity: critical, status: firing, summary: guard,
           description: "", namespace: preprod-kingdom1, labels: {alertname: Guard},
           annotations: {}, starts_at: "2026-08-10T10:00:00Z", ends_at: null,
           generator_url: null, raw: {}}
intent:
  action: restart_deployment
  resource_type: deployment
  resource_name: bot-service
  namespace: preprod-kingdom1
  risk: low
expect:
  # Дефолт политики — BLOCK, а не approve: неизвестный tier не должен
  # молча превращаться в разрешение.
  gate: block
