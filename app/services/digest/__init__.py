"""Слои дайджеста, вынесенные из app/services/stats_digest.py.

Файл дайджеста рос до 3457 строк, и в нём вперемешку жили три разные вещи:
сборка текста секций (собственно дайджест), межпрогонное состояние в Redis и
чистое форматирование чисел. Первое меняется при каждой правке отчёта, второе
переживает перезапуск воркера, третье не зависит вообще ни от чего.

  * `state`      — снапшоты предыдущего дня и heartbeat beat-тасков (Redis);
  * `formatting` — чистые форматтеры (Δ, тренды, «5m ago», маркеры);
  * `failures`   — учёт секций, не собравшихся в текущем прогоне.

Секции остались в `stats_digest`: они и есть дайджест.
"""
