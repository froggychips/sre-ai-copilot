"""Vendored copy of `teamcity_mcp` client.

Источник: ~/projects/teamcity-mcp (внутренний пакет, не на PyPI и не в git).
Сюда внесён только `client.py` — нужный для TC REST из `teamcity_service.py`.
При изменении upstream — синхронизировать вручную.
"""
