"""Провайдеры внешних источников: логи, метрики, деплои, топология.

Первый интерфейс — `LogProvider`, и он первый не случайно. У логов уже
есть две реальные реализации в виду (Seq сегодня, ClickHouse завтра), а
значит границу можно проверить, а не придумать: абстракция, выведенная из
одной реализации, обычно повторяет её форму и протекает на второй.

Интерфейсы выводятся из того, что реально спрашивает копайлот, а не из
того, что умеет источник. Поэтому наружу отдаются агрегаты и измерения, а
не сырые события конкретной системы: raw-событие Seq — деталь Seq, и
провайдер поверх ClickHouse честно реализовать его бы не смог.
"""
