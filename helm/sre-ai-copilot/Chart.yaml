apiVersion: v2
name: sre-ai-copilot
description: Python FastAPI + Celery SRE incident-response copilot
type: application
version: "0.1.0"
# Версия, которая РЕАЛЬНО крутится в ns sre-ai. Значение описательное:
# деплой идёт не helm-ом, а зеркалированием ghcr-образа по sha во внутренний
# реестр + `kubectl set image` (helm list -n sre-ai пуст, чарт не установлен).
# Держим в актуальном состоянии, чтобы репозиторий не врал: до 06.08.2026 тут
# стояло 0.5.0, когда в кластере давно работал 1.0.0-rc.7.
# В футере алертов версия берётся НЕ отсюда, а из env BUILD_VERSION.
appVersion: "1.0.0-rc.8"
keywords:
  - sre
  - ai
  - copilot
  - incident-response
maintainers:
  - name: froggychips
